Review and refine code for $ARGUMENTS
Follow these rules:
- Identify any inefficient code, prioritising quick fixes for smoother runtime.
- Clear out unused vars or redundant functions/modules.
- Outline refinement plan with user before applying changes
- Carry out fixes in phases, startign with most critical.
- Test where possible, if GUI tests are needed ask the user to test and provide logs.
- Don't consider fixes succesful until user confirms - even if tested by you, share results for confirmation.
- Once fixes are user confirmed, clear out any debug/testing scripts on completion.
- Commit to git.